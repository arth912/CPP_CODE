#include<stdio.h>
int main()
{
/*	long long int t,j=0,n,fact;
	scanf("%lld",&t);
	long long int a[t],i;
	for(i=0;i<t;i++)
	{	scanf("%lld",&n);
		a[i]=n;
	}
	for(j=0;j<t;j++)	
	{
		n=a[j];
		fact=1;
		for(i=1;i<=n;i++)
		{
			fact=fact*i;
		}
		a[j]=fact;
	}

	 
	 for(j=0;j<t;j++)
		{
		printf("%lld\n",a[j]);
 		}
	 return 0;*/
	 long long int n;
	 scanf("%lld",&n);
	 printf("%lld",n);
}

